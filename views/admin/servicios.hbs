
<main class="holder-01">
    <div class="container" style="margin-top: 50px;">
        <h3>Hola, {{person}}! <a class="btn btn-info" href="/admin/login/logout">Cerrar sesión <i class="fas fa-sign-out-alt"></i></a></h3>
    </div>
        <div class="row">
            <div class="col-7">
                <h4>Listado de Nuevos Servicios</h4>
            </div>
            <div class="col-5 text-end">
                <a class="btn" href="/admin/servicios/agregar"><i class="fa fa-plus" aria-hidden="true">Nuevo</i></a>
            </div>
        </div>
    <div class="row">
        <div class="col">
            <table class="table">
                <thead>
                    <tr class="table-dark">
                        <th scope="col">#</th>
                        <th scope="col">Titulo</th>
                        <th scope="text-end">Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each servicios}} {{!- MUY IMPORTANTE DISTINGUIR MAYUSCULAS DE MINUSCULAS, o el codigo no va a funcionar. Mismo cuando se conectan con elementos de BD}}
                    <tr>
                        <th scope="row">{{id}}</th>
                        <td>{{{titulo}}}</td>
                        <td class="text-end">
                            <a class="btn" href="/admin/servicios/modificar{{id}}"><i class="fa fa-pencil" aria-hidden="true"></i></a>
                            <a class="btn" href="/admin/servicios/eliminar/{{id}}"><i class="fa fa-trash" aria-hidden="true"></i></a>
                        </td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>
    </div>
</main>